# Placeholder — se completará al implementar el módulo frontend.
# Ver prompts/ y definicion-inicial.md §7.
FROM node:20-alpine

WORKDIR /app

# TODO(frontend): copiar package.json e instalar.
# TODO(frontend): copiar código de la app.
# TODO(frontend): CMD ["npm", "run", "dev"]

CMD ["node", "-e", "console.log('frontend placeholder — pendiente de implementar'); setInterval(()=>{}, 1<<30);"]
