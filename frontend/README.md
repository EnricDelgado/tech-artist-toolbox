# Frontend

Next.js 15 + React 19 + TypeScript + Tailwind. Consume tanto `calc-core`
(para cálculo en cliente) como la API REST del backend.

Estructura pendiente:

```
frontend/
├── app/
│   ├── page.tsx
│   ├── calculators/
│   ├── projects/
│   └── layout.tsx
├── components/
├── lib/
├── tests/
├── package.json
└── Dockerfile
```

Se genera en el siguiente hito. Ver `../definicion-inicial.md` §7.
