# Backend

FastAPI + Python 3.12. Expone la API REST v1 y persiste los perfiles de
proyecto en PostgreSQL.

Estructura pendiente:

```
backend/
├── app/
│   ├── main.py
│   ├── api/v1/
│   ├── calc/           # port de calc-core a Python
│   ├── models/
│   └── schemas/
├── tests/
├── alembic/
├── pyproject.toml
└── Dockerfile
```

Se genera en el siguiente hito. Ver `../definicion-inicial.md` §7 y §9.
